# RustScript VS Code Extension

Language support for RustScript (`.rss`) files.

## Features

- RustScript file association
- TextMate grammar highlighting
- Bracket and comment configuration

## Build

```bash
npx --yes @vscode/vsce package --out dist/rustscript-vscode.vsix
```
