/// Host function `http::downstream::attach_transport`.
#[pd_host_function(name = "http::downstream::attach_transport")]
fn http_downstream_attach_transport() {
    unreachable!("abi declaration only")
}
