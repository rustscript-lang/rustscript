[package]
name = "pd-vm-runtime-wasm"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
vm = { package = "pd-vm", path = "..", default-features = false, features = ["runtime"] }
